// app/search-products/SearchProductsClient.tsx
"use client";

// ✅ ここから下に「今の app/search-products/page.tsx の中身」を移してくる
// 例：useSearchParams / useRouter / state / UI など全部ここへ

export default function SearchProductsClient() {
    return (
        <main className="mx-auto max-w-6xl px-4 py-10">
            {/* ここに検索UI */}
        </main>
    );
}
